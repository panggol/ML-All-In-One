# =============================================================================
# ML All In One - Docker Compose
# =============================================================================
# Usage:
#   docker compose up -d          # Start API server
#   docker compose run --rm mlkit python examples/train_sklearn.py
#   docker compose run --rm mlkit python examples/example_auth.py
# =============================================================================

services:
  mlkit:
    build: .
    image: mlkit:latest
    container_name: mlkit
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./checkpoints:/app/checkpoints
      - ./experiments:/app/experiments
    working_dir: /app
    environment:
      - PYTHONPATH=/app/src
    networks:
      - mlkit-net

  api:
    build: .
    image: mlkit:latest
    container_name: mlkit-api
    command: uvicorn mlkit.api.inference:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./checkpoints:/app/checkpoints
      - ./experiments:/app/experiments
    working_dir: /app
    environment:
      - PYTHONPATH=/app/src
    networks:
      - mlkit-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/docs"]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    image: node:20-alpine
    container_name: mlkit-frontend
    working_dir: /app
    volumes:
      - ./frontend:/app
    ports:
      - "3000:3000"
    environment:
      - API_PROXY_TARGET=http://api:8000
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    networks:
      - mlkit-net
    depends_on:
      - api

networks:
  mlkit-net:
    driver: bridge
